from openai import OpenAI

client = OpenAI(
    base_url="{{baseUrl}}",
    api_key="{{apiKey}}",    
)

run = client.beta.threads.runs.retrieve(
    run_id="<YOUR_RUN_ID>", 
    thread_id="<YOUR_THREAD_ID>", 
)

print(f"Run: {run}")
