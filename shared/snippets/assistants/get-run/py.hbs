from openai import OpenAI

client = OpenAI(
    base_url="{{baseUrl}}",
    api_key="{{apiKey}}",    
)

run = client.beta.threads.runs.retrieve("<YOUR_THREAD_ID>", "<YOUR_RUN_ID>")

print(f"Run: {run}")
