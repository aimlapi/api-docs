const main = async () => {
  const params = new URLSearchParams({
    followup_id: '<YOUR_GENERATION_ID>'
  });
  const response = await fetch(`{{baseUrl}}/bagoodex/images?${params.toString()}`, {
    method: 'GET',
    headers: {
      Authorization: 'Bearer {{apiKey}}',
      'Content-Type': 'application/json',
    },
  }).then((res) => res.json());

  console.log('Generation:', response);
};

main();
